# Análise estatística de base de dados referentes à diabetes
FAZER UM TEXTO

![imagens](imagens/diabetes.jpeg)



## Organização do projeto

```

├── .gitignore         <- Arquivos e diretórios a serem ignorados pelo Git
├── ambiente.yml       <- O arquivo de requisitos para reproduzir o ambiente de análise
├── LICENSE            <- Licença de código aberto (MIT)
├── README.md          <- README principal para desenvolvedores que usam este projeto.
|
├── dados              <- Arquivos de dados para o projeto.
|

├── notebooks          <- Cadernos Jupyter
│
|   └──src             <- Código-fonte para uso neste projeto.
|      │
|      ├── __init__.py  <- Torna um módulo Python
|      ├── config.py    <- Configurações básicas do projeto
|      └── estatística.py  <- Funções desenvolvidas especificamente para este projeto
|
├── referencias        <- Dicionários de dados
├── imagens       <- Imagens utilizadas no projeto
```



## Configuração do amiente

1. Faça o clone do repositório que será criado a partir deste modelo.

    ```bash
    git clone git@github.com:exemplo/modelo_projeto_data_science.git
    ```

2. Crie um ambiente virtual para o seu projeto utilizando o "conda"

     Caso esteja utilizando o `conda`, exporte as dependências do ambiente para o arquivo `ambiente.yml`:

      ```bash
      conda env create -f ambiente.yml > ambiente.yml

      ```
## DISCORRER SOBRE A BASE
[Clique aqui](referencias/01_dicionario_de_dados.md) 

##Resumo das principais conclusões

 A FAZER
    
